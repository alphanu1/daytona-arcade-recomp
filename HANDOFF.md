# Handoff

## Current state

Design only. `docs/daytona-usa-recomp-design.md` and `rules.md` exist; no code,
tooling or build yet.

## Complete

- Design document.
- Project rules (`rules.md`) and `.gitignore`.

## Next, in order

M0 tooling (design doc, Milestones):

1. Confirm the "from memory" figures in the Target hardware summary against
   MAME's `sega/model2.cpp` — CPU clock, refresh rate, resolution, IRQ order.
2. MAME trace plugin, input recorder, trace diff tool, i960 disassembler.

## Open decisions

- Project licence. The Model 2 MiSTer core is GPL-3; lifting from it decides
  this.
- MiSTer core repository URL for the design doc (local copy:
  `/home/ben/source/sega-model2-mister`).

## Findings

None yet.

## What not to re-propose

Nothing yet.
